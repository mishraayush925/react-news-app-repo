import React from "react";
import { useParams } from 'react-router-dom';

export function Newsdetails () {
   // var { id } = useParams(); // Get the id from the URL
   // var id;

    return (
        <>
        </>
    );
};

//export default newsDetails;
